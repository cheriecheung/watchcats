import styled from 'styled-components';

const color = '#487364';

const CardTitle = styled.div`
  font-size: 1.1rem;
  font-weight: 800;
  color: ${color};
  margin-bottom: 20px;
`;

export default CardTitle;
