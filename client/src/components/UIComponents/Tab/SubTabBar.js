import styled from 'styled-components';

const SubTabBar = styled.div`
  width: 100%;
  height: 50px;
  overflow-x: scroll;
  margin-top: 25px;
  white-space: nowrap;

  ::-webkit-scrollbar {
    display: none;
  }
`

export default SubTabBar