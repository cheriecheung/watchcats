import styled from 'styled-components';

const ImageContainer = styled.div`
  width: 100px;
  height: 100px;
  background: pink;
  overflow: hidden;
  border-radius: 10px;
`;

export default ImageContainer;
